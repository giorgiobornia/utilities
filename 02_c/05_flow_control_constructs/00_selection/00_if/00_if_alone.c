#include <stdio.h>

/* It is better programming practice to always think of the alternative, so to add an "else" statement */

int main() {

    int j = 8;
    
    if (j == 8) printf("Hello world\n");
    
    return 0;
}
