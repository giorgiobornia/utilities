/* return is an instruction that relates to the FUNCTION that contains it.
   The compiler always controls the type of the returned expression with the return value of the function */

#include <stdio.h>

int main() {
    
   return 7;
   
   printf("Hello world\n");
    
   return 0;
}
